# 贡献指南 · Contributing

感谢你有兴趣让这个技能更可靠。法律类项目**准确性远比功能数量重要**，
因此本项目的贡献流程对"可核验性"有明确要求。

## 一、提交前必做

```bash
python3 scripts/selftest.py      # 必须全部通过
python3 scripts/law.py verify    # 语料必须 0 个问题
```

CI 会在 push/PR 时自动运行上述两项，未通过不予合并。

## 二、你可以贡献什么

| 类型 | 说明 |
| --- | --- |
| **新增法规语料** | 补充劳动、公司、民事之外的常用规范（如破产法、票据法、行政处罚法等） |
| **修法更新** | 法律被修正/修订后，更新对应语料文件与版本信息 |
| **纠正错误** | 抓取错误、条文残缺、条号错位、错别字 |
| **检索词优化** | 补充"口语 → 法言法语"对照表（`references/query-expansion.md`） |
| **实务知识** | 补充 playbook 中的检索地图、地方口径提示（**不得写无条号支撑的结论**） |
| **工具改进** | `law.py` 的功能与健壮性 |
| **文档与翻译** | 改进 README、补充英文文档 |

## 三、新增/更新法规语料

### 方式一：用脚本（推荐）

```bash
python3 scripts/add_document.py \
  --title "中华人民共和国企业破产法" --short "企业破产法" --category company \
  --version "2006年8月27日第十届全国人民代表大会常务委员会第二十三次会议通过" \
  --effective 2007-06-01 \
  --source "https://www.gov.cn/..." \
  --slug enterprise-bankruptcy-law \
  --from-url "https://www.gov.cn/..."
```

脚本会：去 HTML 标签 → 清理噪音 → 校验条号连续性 → 写入 `references/raw/` → 复核解析结果。

### 方式二：手工准备文本

在 `references/raw/<slug>.txt` 中写入统一头部：

```
---
title: 法规全称
short: 简称
category: labor | company | civil | procedure | other
version: 通过/修正/修订说明（含日期与文号）
effective: 施行日期 YYYY-MM-DD
source: 抓取来源 URL
source2: 交叉验证来源 URL（可选，强烈建议）
fetched: 抓取日期 YYYY-MM-DD
articles: 条文总数
status: 现行有效 | 已修改 | 已废止
note: 需要提醒使用者的特殊事项（可选）
---
```

随后空一行，接正文（从"第一条"到末条，保留"第X编/章/节"标题）。
**不设条号的批复、决定类文件**请设 `articles: 0` 并加 `--allow-unnumbered`。

### 硬性要求

1. **必须来自官方来源**：`*.gov.cn`、`*.npc.gov.cn`、`*.court.gov.cn`、`*.spp.gov.cn`、
   `*.mohrss.gov.cn` 等。地方政府/人大官网转载的官方文本可作**次选**，
   但**必须用第二个独立来源交叉验证**，并在 `source2` 中记录。
2. **禁止凭记忆生成或补全**。抓不到就如实说明，不得编造条文。
3. **不得改动条文内容**。只允许做无争议的规范化：去 HTML 标签、合并多余空白、
   把连排的"第X条正文"补一个空格、剔除网页导航/目录/记者署名等噪音。
   任何"规范化"都必须在 PR 描述中说明。
4. **条号必须连续**（1..N 无缺号无重复），且与头部 `articles` 一致。
5. **确认是截至当前的最新有效版本**；若为修正决定而非全文，不要提交。

### 提交时请附

- 每部法规的来源 URL（及交叉验证 URL）；
- 条数、末条条号；
- 你做的任何文本规范化说明；
- 若发现官方版本与预期条数不符，请说明核实过程。

## 四、修改技能指令（SKILL.md 等）

- 保持"**先查后答**"与"**先问后答**"两条主线不被削弱；
- 新增的实务规则**必须可追溯到具体条号**，并在示例中体现；
- 不要引入无条号支撑的绝对化结论（如"一定能赢""必然赔偿 2 倍"）；
- 涉及地方标准的内容，必须写成"需按当地口径核验"，而非给出具体数字。

## 五、代码规范

- `scripts/*.py`：**仅使用 Python 3.8+ 标准库**（零第三方依赖是本项目的核心承诺）；
- 保持"递增条号切分"等既有算法行为，修改解析逻辑须同步补充 `selftest.py` 用例；
- 提交信息建议使用约定式前缀：`feat:` / `fix:` / `docs:` / `corpus:` / `test:`。

## 六、报告问题

请使用 Issue 模板，并尽量附上：

- 复现命令（如 `python3 scripts/law.py article 劳动合同法 38`）；
- 预期结果与实际结果；
- 若涉及法条内容，请附**官方来源链接**。

## 七、行为准则

保持专业与克制。本项目讨论的是法律文本，请勿在 Issue/PR 中提供针对具体个案的法律意见，
也不要索取或提供他人隐私信息。
